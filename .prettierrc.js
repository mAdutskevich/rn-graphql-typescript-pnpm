module.exports = {
    singleQuote: true, // default: false. Use single quotes instead of double quotes.
    // semi: true, // default: true. Print semicolons at the ends of statements.
    printWidth: 100, // default: 80. Specify the line length that the printer will wrap on.
    tabWidth: 4, // default: 2. Specify the number of spaces per indentation-level.
    // useTabs: false, // default: false. Indent lines with tabs instead of spaces.
    trailingComma: 'all', // default: 'es5'. Print trailing commas wherever possible in multi-line comma-separated syntactic structures.
    proseWrap: 'always', // default: 'preserve'. Prettier will change wrapping in markdown text(because of always value).
    // bracketSpacing: true, // default: true. Print spaces between brackets in object literals.
    // bracketSameLine: false, // default: false. Put the > of a multi-line HTML (HTML, JSX, Vue, Angular) element at the end of the last line instead of being alone on the next line
};
